<h1>All my tasks</h1>
<% @tasks.each do |task| %>
  <% if task.completed %>
    <h3>[X] <%= link_to task.title, task_path(task) %></h3>
  <% else %>
  <h3>[ ] <%= link_to task.title, task_path(task) %></h3>
  <% end %>


<% end %>

  <%= link_to "Add a new task", tasks_new_path  %>


